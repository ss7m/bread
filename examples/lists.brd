# initialize a list of length 100 of all zeros
#set x = for i=0,100 do 0 end

# 2d array of all zeros
#set y = for i=0,100 do for j = 0,100 do 0 end end

set y = [ "ab".."ab", "ab".."ab", "ab".."ab" ]
set x = [ y, y, "Hello, " .. "World!" ]
@writeln( "x = ", x )
set y = 1
4
@writeln( "x[0] = ", x[0] )
set x[0][1] = "wow!"
@writeln( "x = ", x )

set x = []
@writeln( x or "this list empty. YEET" )

set i = 0
set x = while i < 10 do
  -1 + (set i = i + 1)
end
@writeln(x)

@writeln("indexing a string: ", "hello"[1])
