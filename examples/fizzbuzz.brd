set x = 1

while x <= 45 do
  set three = x % 3 = 0
  set five  = x % 5 = 0

  @writeln (
    if three and five then
      "fizzbuzz"
    elif three then
      "fizz"
    elif five then
      "buzz"
    else
      x
    end
  )

  set x = x + 1
end
