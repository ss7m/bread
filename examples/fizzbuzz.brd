set x = 1

while x < 10000 do
  set three = x % 3 = 0
  set five  = x % 5 = 0

  @writeln (
    if three or five then
      three and "fizz" or "" .. five and "buzz" or ""
    else
      x
    end
  )

  set x = x + 1
end
