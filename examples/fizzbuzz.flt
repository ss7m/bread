set x = 1;

while x <= 100 do
    set three = x % 3 = 0;
    set five = x % 5 = 0;

    if three or five then
        @writeln(
            if three then "fizz"; else ""; end,
            if five  then "buzz"; else ""; end
        );
    else
        @writeln(x);
    end;
end;
